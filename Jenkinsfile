pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Build stage running'
                sh 'sleep 2'
            }
        }

        stage('Test') {
            steps {
                echo 'Test stage running'
                sh 'sleep 2'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy stage running'
                sh 'sleep 2'
            }
        }
    }
}
